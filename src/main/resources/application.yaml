spring:
  application:
    name: CrudSecurityRestApplication
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/postgres
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always

jwt_secret: J37$Iv9*As6@